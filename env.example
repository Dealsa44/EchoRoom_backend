# Driftzo Backend Environment Variables

# Server Configuration
PORT=5000
NODE_ENV=production

# Database (Supabase PostgreSQL)
# Pooled URL (for app) - use Session pooler in Supabase Connect modal
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"
# Direct URL (for migrations)
DIRECT_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# JWT Secret (generate a random string)
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# Email (Resend) - after verifying driftzo.com at https://resend.com/domains
RESEND_API_KEY=
RESEND_FROM=Driftzo <noreply@driftzo.com>

# Frontend URL (for CORS) - Production URL
FRONTEND_URL=https://echo-room-ashen.vercel.app

# Note: The backend now automatically allows these development URLs:
# - http://localhost:8081 (your current dev port)
# - http://localhost:5173 (default Vite port)
# - http://localhost:3000 (common React port)

# Email Verification
VERIFICATION_CODE_EXPIRY=600000
VERIFICATION_RESEND_COOLDOWN=60000
